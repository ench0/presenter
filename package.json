{
  "name": "presenter",
  "version": "0.1.0",
  "description": "Presenter",
  "main": "index.js",
  "scripts": {
    "start": "node ./bin/www",
    "dev": "cross-env NODE_ENV=dev nodemon ./bin/www -e js,css,pug,json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ensar",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^4.0.0",
    "express": "^4.16.2",
    "mysql": "^2.15.0",
    "prayer-timetable": "^0.2.3"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/ench0/js_presenter.git"
  },
  "engines": {
    "node": ">= 7"
  },
  "devDependencies": {
    "cross-env": "^5.0.5"
  }
}
